<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidtemplate">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />       <!--For internet connectivity checking-->
    <uses-permission android:name="android.permission.READ_CONTACTS" />     <!--For content provider demo-->
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />     <!--For content provider demo-->
    <uses-permission android:name="android.permission.CAMERA" />    <!--For Dexter demo-->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />  <!--For Dexter demo-->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />


    <application
        android:name=".util.MyApp"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidTemplate">
        <activity
            android:name=".activities.HomeActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>